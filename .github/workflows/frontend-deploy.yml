name: CI/CD

on:
  push:
    branches:
      - main
    paths:
      - 'front/**'  # front 디렉토리로 경로 수정
  pull_request:
    branches:
      - main
    paths:
      - 'front/**'  # front 디렉토리로 경로 수정

jobs:
  cicd:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: front  # 작업 디렉토리를 front로 수정

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20.10.0
          cache: 'npm'
          cache-dependency-path: 'front/package-lock.json'  # 캐시 경로 수정

      - name: Install dependencies
        run: npm ci

      - name: Set Environment Variables
        run: |
          echo "VITE_TEST_DATA=${{ secrets.VITE_TEST_DATA }}" >> $GITHUB_ENV

      - name: Build
        run: npm run build

      - name: Deploy to S3
        uses: jakejarvis/s3-sync-action@master
        with:
          args: --delete
        env:
          AWS_S3_BUCKET: ${{ secrets.AWS_S3_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          SOURCE_DIR: 'front/dist'  # 빌드 결과물 경로 수정